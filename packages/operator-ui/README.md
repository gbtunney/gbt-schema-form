<!-- Template-inspired header with badges and a succinct description. -->

# @operator/ui

> React components and hooks for the operator user interface

This package contains reusable React components and hooks implementing the
evidence operator UI. It depends on `@operator/core` and `@operator/store` but
does not import any database or AI libraries. See `src/index.tsx` for the
entry point and placeholder implementation.
