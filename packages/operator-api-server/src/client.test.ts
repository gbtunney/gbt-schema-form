// the generated file

import { describe, expect, it } from 'vitest'

describe('operator-api placeholder', () => {
    it('should be defined', () => {
        ///  const _client = new client(/* optional custom Implementation */);
        //client.provide("get /v1/hello", { id: "10" });
        expect(true).toBe(true)
    })
})
