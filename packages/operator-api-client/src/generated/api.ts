/**
 * AUTO-GENERATED FILE — DO NOT EDIT.
 *
 * ## Generated by: express-zod-api on 02/26/2026, 5:12:59 AM
 *
 * Build: 2026-02-26T10:12:59.331Z To regenerate: pnpm run gen:client
 */
// @generated

type SomeOf<T> = T[keyof T]

/** Get /derive/ocr */
type GetDeriveOcrInput = {
    base64?: string | undefined
    imageUrl?: string | undefined
    langs?: (string | Array<string>) | undefined
}

/** Get /derive/ocr */
type GetDeriveOcrPositiveVariant1 = {
    status: 'success'
    data: {
        confidence: number
        text: string
    }
}

/** Get /derive/ocr */
type GetDeriveOcrPositiveResponseVariants = {
    200: GetDeriveOcrPositiveVariant1
}

/** Get /derive/ocr */
type GetDeriveOcrNegativeVariant1 = {
    status: 'error'
    error: {
        message: string
    }
}

/** Get /derive/ocr */
type GetDeriveOcrNegativeResponseVariants = {
    400: GetDeriveOcrNegativeVariant1
}

/** Head /derive/ocr */
type HeadDeriveOcrInput = {
    base64?: string | undefined
    imageUrl?: string | undefined
    langs?: (string | Array<string>) | undefined
}

/** Head /derive/ocr */
type HeadDeriveOcrPositiveVariant1 = undefined

/** Head /derive/ocr */
type HeadDeriveOcrPositiveResponseVariants = {
    200: HeadDeriveOcrPositiveVariant1
}

/** Head /derive/ocr */
type HeadDeriveOcrNegativeVariant1 = undefined

/** Head /derive/ocr */
type HeadDeriveOcrNegativeResponseVariants = {
    400: HeadDeriveOcrNegativeVariant1
}

/** Get /hello */
type GetHelloInput = {
    name?: string | undefined
    nickname: string
}

/** Get /hello */
type GetHelloPositiveVariant1 = {
    status: 'success'
    data: {
        greetings: string
    }
}

/** Get /hello */
type GetHelloPositiveResponseVariants = {
    200: GetHelloPositiveVariant1
}

/** Get /hello */
type GetHelloNegativeVariant1 = {
    status: 'error'
    error: {
        message: string
    }
}

/** Get /hello */
type GetHelloNegativeResponseVariants = {
    400: GetHelloNegativeVariant1
}

/** Head /hello */
type HeadHelloInput = {
    name?: string | undefined
    nickname: string
}

/** Head /hello */
type HeadHelloPositiveVariant1 = undefined

/** Head /hello */
type HeadHelloPositiveResponseVariants = {
    200: HeadHelloPositiveVariant1
}

/** Head /hello */
type HeadHelloNegativeVariant1 = undefined

/** Head /hello */
type HeadHelloNegativeResponseVariants = {
    400: HeadHelloNegativeVariant1
}

/** Post /hello */
type PostHelloInput = {
    name?: string | undefined
    nickname: string
}

/** Post /hello */
type PostHelloPositiveVariant1 = {
    status: 'success'
    data: {
        greetings: string
    }
}

/** Post /hello */
type PostHelloPositiveResponseVariants = {
    200: PostHelloPositiveVariant1
}

/** Post /hello */
type PostHelloNegativeVariant1 = {
    status: 'error'
    error: {
        message: string
    }
}

/** Post /hello */
type PostHelloNegativeResponseVariants = {
    400: PostHelloNegativeVariant1
}

export type Path = '/derive/ocr' | '/hello'

export type Method = 'get' | 'post' | 'put' | 'delete' | 'patch' | 'head'

export type Input = {
    'get /derive/ocr': GetDeriveOcrInput
    'head /derive/ocr': HeadDeriveOcrInput
    'get /hello': GetHelloInput
    'head /hello': HeadHelloInput
    'post /hello': PostHelloInput
}

export type PositiveResponse = {
    'get /derive/ocr': SomeOf<GetDeriveOcrPositiveResponseVariants>
    'head /derive/ocr': SomeOf<HeadDeriveOcrPositiveResponseVariants>
    'get /hello': SomeOf<GetHelloPositiveResponseVariants>
    'head /hello': SomeOf<HeadHelloPositiveResponseVariants>
    'post /hello': SomeOf<PostHelloPositiveResponseVariants>
}

export type NegativeResponse = {
    'get /derive/ocr': SomeOf<GetDeriveOcrNegativeResponseVariants>
    'head /derive/ocr': SomeOf<HeadDeriveOcrNegativeResponseVariants>
    'get /hello': SomeOf<GetHelloNegativeResponseVariants>
    'head /hello': SomeOf<HeadHelloNegativeResponseVariants>
    'post /hello': SomeOf<PostHelloNegativeResponseVariants>
}

export type EncodedResponse = {
    'get /derive/ocr': GetDeriveOcrPositiveResponseVariants &
        GetDeriveOcrNegativeResponseVariants
    'head /derive/ocr': HeadDeriveOcrPositiveResponseVariants &
        HeadDeriveOcrNegativeResponseVariants
    'get /hello': GetHelloPositiveResponseVariants &
        GetHelloNegativeResponseVariants
    'head /hello': HeadHelloPositiveResponseVariants &
        HeadHelloNegativeResponseVariants
    'post /hello': PostHelloPositiveResponseVariants &
        PostHelloNegativeResponseVariants
}

export type Response = {
    'get /derive/ocr':
        | PositiveResponse['get /derive/ocr']
        | NegativeResponse['get /derive/ocr']
    'head /derive/ocr': PositiveResponse['head /derive/ocr']

    'get /hello':
        | PositiveResponse['get /hello']
        | NegativeResponse['get /hello']
    'head /hello': PositiveResponse['head /hello']

    'post /hello':
        | PositiveResponse['post /hello']
        | NegativeResponse['post /hello']
}

export type Request = keyof Input

export const endpointTags = {
    'get /derive/ocr': [],
    'get /hello': [],
    'head /derive/ocr': [],
    'head /hello': [],
    'post /hello': [],
}

const parseRequest = (request: string) =>
    request.split(/ (.+)/, 2) as [Method, Path]

const substitute = (path: string, params: Record<string, any>) => {
    const rest = { ...params }
    for (const key in params) {
        path = path.replace(`:${key}`, () => {
            delete rest[key]
            return params[key]
        })
    }
    return [path, rest] as const
}

export type Implementation<T = unknown> = (
    method: Method,
    path: string,
    params: Record<string, any>,
    ctx?: T,
) => Promise<any>

const defaultImplementation: Implementation = async (method, path, params) => {
    const hasBody = !['get', 'head', 'delete'].includes(method)
    const searchParams = hasBody ? '' : `?${new URLSearchParams(params)}`
    const response = await fetch(
        new URL(`${path}${searchParams}`, 'https://example.com'),
        {
            body: hasBody ? JSON.stringify(params) : undefined,
            headers: hasBody
                ? { 'Content-Type': 'application/json' }
                : undefined,
            method: method.toUpperCase(),
        },
    )
    const contentType = response.headers.get('content-type')
    if (!contentType) return
    const isJSON = contentType.startsWith('application/json')
    return response[isJSON ? 'json' : 'text']()
}

export class Client<T> {
    public constructor(
        protected readonly implementation: Implementation<T> = defaultImplementation,
    ) {}
    public provide<K extends Request>(
        request: K,
        params: Input[K],
        ctx?: T,
    ): Promise<Response[K]> {
        const [method, path] = parseRequest(request)
        return this.implementation(method, ...substitute(path, params), ctx)
    }
}

export class Subscription<
    K extends Extract<Request, `get ${string}`>,
    R extends Extract<PositiveResponse[K], { event: string }>,
> {
    public source: EventSource
    public constructor(request: K, params: Input[K]) {
        const [path, rest] = substitute(parseRequest(request)[1], params)
        const searchParams = `?${new URLSearchParams(rest)}`
        this.source = new EventSource(
            new URL(`${path}${searchParams}`, 'https://example.com'),
        )
    }
    public on<E extends R['event']>(
        event: E,
        handler: (
            data: Extract<R, { event: E }>['data'],
        ) => void | Promise<void>,
    ) {
        this.source.addEventListener(event, (msg) =>
            handler(JSON.parse(msg.data)),
        )
        return this
    }
}

// Usage example:
/*
const client = new Client();
client.provide("get /v1/user/retrieve", { id: "10" });
new Subscription("get /v1/events/stream", {}).on("time", (time) => {});
*/
